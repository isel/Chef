require 'rake'
require 'rspec/core/rake_task'

puts "server type = <%= @server_type %>"

ENV['deployment.name'] = '<%= @deployment_name %>'
ENV['domain.name'] = ''
#ENV['web_revision'] = ''

RSpec::Core::RakeTask.new(:spec) do |t|
  t.pattern = '/DeployScripts/CI/spec/Smoke/deploy_spec.rb'
end

task :default => [:spec]